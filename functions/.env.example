# Stripe — secret só no backend (NUNCA no app)
STRIPE_SECRET_KEY=sk_test_...
# Webhook signing secret (Dashboard → Developers → Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_...
# Comissão Setmatch % (0 = desligada)
STRIPE_PLATFORM_FEE_PERCENT=0
# Legado Mercado Pago (opcional)
MP_ACCESS_TOKEN=
# Strava OAuth (Cloud Functions — stravaExchangeCode / stravaSyncToday)
STRAVA_CLIENT_ID=
GOOGLE_MAPS_API_KEY=
# Places API (Nearby + Text Search) — chave só no backend
