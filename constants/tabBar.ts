/** Altura da barra (64) + margem lateral/bottom mínima — usar + insets.bottom nas telas. */
export const TAB_BAR_HEIGHT = 64;

/** Clearance fixo seguro quando não há acesso a insets (≈ barra + home indicator). */
export const TAB_BAR_CLEARANCE = 112;
