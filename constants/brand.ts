/** Nome de marca exibido ao usuário (Figma: Rally Up). */
export const APP_DISPLAY_NAME = 'Rally Up';

/** Prefixo amigável do ID de atleta (campo Firestore permanece `setmatchId`). */
export const ATHLETE_ID_LABEL = 'ID Rally Up';
